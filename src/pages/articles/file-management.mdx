import { ArticleLayout } from '@/components/ArticleLayout'

export const meta = {
  author: 'William Pepple',
  date: '2023-02-18',
  title: 'File Management Hacks',
  description:
    'Linux commands for file management',
}

export default (props) => <ArticleLayout meta={meta} {...props} />


```
    rm -r -f            removes files and directory

    du -s               disk usage

    mv -f -i            moves files or directory



```

